<%= form_with(model: scope) do |form| %>
  <% if scope.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scope.errors.count, "error") %> prohibited this scope from being saved:</h2>

      <ul>
        <% scope.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
