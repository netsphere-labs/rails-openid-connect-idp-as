
<% # @param client
   # @param redirect_uris
   # @param f
  %>

  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
        <% client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class="uk-margin">
  <%= f.label :name, :class => 'uk-form-label' %>
  <div class="uk-form-controls">
    <%= f.text_field :name, :class => 'uk-input' %>
  </div>
</div>

<% redirect_uris.each do |redirect_uri| %>
<div class="uk-margin">
  <%= f.label :redirect_uri, :class => 'uk-form-label' %>
  <div class="uk-form-controls">
    <%= url_field_tag 'client[redirect_uris][]', redirect_uri, :class => 'uk-input' %>
  </div>
</div>
<% end %>

<div class="uk-margin">
  <%= f.label :redirect_uri, :class => 'uk-form-label' %>
  <div class="uk-form-controls">
    <%= url_field_tag 'client[redirect_uris][]', '', :class => 'uk-input' %>
  </div>
</div>
